--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@
-option(ENABLE_OPENGL "Enable OpenGL support" ON)
+option(ENABLE_OPENGL "Enable OpenGL support" OFF)

# Kodi refuses to build PAPlayer without a renderer; trick it:
-set(APP_RENDER_SYSTEM "egl")
+set(APP_RENDER_SYSTEM "offscreen")
